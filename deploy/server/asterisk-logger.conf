[general]
dateformat=%F %T.%3q
queue_log=no

[logfiles]
; Keep console output available to an interactive administrator only.
; The systemd unit drops stdout/stderr, so no persistent Asterisk file is created.
console => notice,warning,error
